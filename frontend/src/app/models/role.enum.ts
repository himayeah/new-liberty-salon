/**
 * Application-wide role definitions.
 * Used for role-based access control across the frontend.
 */
export enum Role {
  OWNER = 'OWNER',
  MANAGER = 'MANAGER',
  RECEPTIONIST = 'RECEPTIONIST',
  STYLIST = 'STYLIST',
  CLIENT = 'CLIENT'
}
