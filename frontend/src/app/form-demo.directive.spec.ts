import { FormDemoDirective } from './form-demo.directive';

describe('FormDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new FormDemoDirective();
    expect(directive).toBeTruthy();
  });
});
