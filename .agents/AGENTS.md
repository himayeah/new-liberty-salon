# Code Formatting Rules

- **Respect Single-Line Layouts**: If the user formats code blocks or specific statements to be on a single line, do not format them back or split them into multiple lines during subsequent edits or refactoring. Preserve the single-line structure exactly as the user has laid it out.
